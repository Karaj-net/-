# Теория
## Назначение  класса. Описание  класса (поля, свойства, автоматические свойства, методы, конструкторы).
**Назначение классов**
Класс в объектно-ориентированном программировании — это шаблон, который описывает свойства и поведение объектов.
**Инкапсуляция данных:**  
Класс служит контейнером для хранения данных (поля). С помощью модификаторов доступа (private, public, protected) можно ограничить доступ к данным и управлять их изменением через методы и свойства. Это помогает защитить данные от некорректного использования.
**Моделирование сущностей реального мира:**  
Класс представляет собой абстракцию объекта. Он описывает его характеристики (свойства) и действия (методы), которые можно выполнять.
**Повторное использование кода:**  
Класс позволяет создавать множество объектов с одинаковой структурой и поведением. Это упрощает разработку, улучшает читаемость кода и снижает вероятность ошибок. Например, вместо описания каждого автомобиля вручную, вы создаете класс Car и используете его для создания объектов.
## Описание класса
**Поля**: используются для хранения данных. Поля обычно делают private, чтобы ограничить прямой доступ.
**Свойства**: Инкапсулируют доступ к полям.
Могут включать логику в методах get и set (например, проверка значения).
Автоматические свойства (Address { get; set; }) автоматически создают поле для хранения данных.
 **Конструктор**: Метод, вызываемый при создании объекта.
Инициализирует поля/свойства объекта.
Методы: задают поведение объекта.
## Классы, как поля других классов, вложенные классы.
Вложенные классы на ровне с ... можно использовать типы соответствующие пользовательскими классами. Мы можем использовать поля класса Student, причем если используемый тип применяется только в пределах данного класса, то можно описание класса записать внутри тела использующего класса

Здесь основная проблема возникает при создании экземпляров или в случае использования конструктора с использованием параметров, так как для заполнения поля, необходимо создать экземпляр класса Student, если мы используем конструктор без параметров, тогда поля заполняются с помощью свойства 
## Структуры. Частичные классы и методы. Анонимные типы.
### Структуры
Структуру можно рассматривать, как некоторый вариант класса, но в отличии от обычного класса, который описывает ссылочный тип, структура описывает значимый тип.
Значимый:
1. описать - выделяется какая-то часть ячеек 
2.  инициализация - заносятся данные в ячейки

Ссылочный:
1. Описать - выделяется фиксированное количество ячеек
2. Создать - экземпляр
3. Инициализация - занос данных

При описании структуры используется `struct` далее аналогично описывается члены класса, здесь важным является, что конструктор без параметра, всегда присутствует и его нельзя переопределить. Не создавая экземпляр, можно использовать свойства для заполнения полей. При помощи конструктора без полей и поля будут инициализированы в начале значениями по умолчанию.
```C#
class StudentCl
{
	private string name;
	private int group;
	public string Name {set {name = value;}}
	public int Group {set {group = value;}}
	public void Pr() => Console.Writeline("Студент {0}, группа {1}", name, group);
	public Student(string, name, int group)
	{
		this.name = name;
		this.group = group;
	}
}

class Program
{
	static void Main()
	{
		StudentCL st1 = new Student("Иванов", 5);
		StudentCl st2 = st1;
		st1.Pr(); //Иванов 5
		st2.Pr(); //Иванов 5
		st2.Name = "Петров";
		st2.Group = 6;
		st1.Pr(); //Петров 6
		st2.Pr(); //Петров 6
		
		StudentST st3 = new StudentST("Иванов", 5);
		StudentST st4 = st3;
		st3.Pr(); //Иванов 5
		st4.Pr(); //Иванов 5
		st4.Name = "Петров";
		st4.Group = 6;
		st3.Pr(); // Иванов 5
		st4.Pr(); //Петров 6
	}
}
```
### Частичные классы и методы
Когда мы добавляем новый класс при помощи меню создается новый класс(шаблон класса), получается, что каждый класс находится в отдельном классе. Можно в принципе добавлять классы друг под другом, не создавая новые файл. Это можно делать при большом количестве классов в учебных целях. Может быть другая ситуация, когда класс надо разбить на 2 файла. Такая необходимость может возникнуть, если разработкой класса занимается несколько людей. В таких случаях мы имеем две части описания одного класса - эти части снабжаются partial и называются частичным классом. Разбиение можно производить произвольным образом. При компиляции они рассматриваются, как единое целое. 
```C#
class Student1
{
	
}
partial class Student
{
	private string name;
	private int group;
	public void Pr()
	{
		Console.Writeline(...);
	}
}
```
Файл2 Student2.cs
```C#
class Student1
{
	
}
partial class Student
{
	public string Name { set {name = value;}}
	public int Group { set {group = value;}}
	
}
```
```C#
class Program
{
	static void Main()
	{
		Student st1 = new Student();
		st1.Name = "Иванов";
		st1.Group = 3;
		st1.Pr(); // Иванов 3
		st1,Pr1(); //Иванов 3
	}
}
```
В частичных классах  могут использоваться частичные методы. Суть этих методов заключается в том, что они описываются в одном из частичных классов, а используются в другом
При использовании частичного метода нудно также указывать ключевое слово partial без модификатора доступа и в том из частичных классов, где он используется, записать заголовок метода.
```C#
partial class Student
{
	public string Name { set {name = value;}}
	public int Group { set{ group = value;}}
	{
		Console.Writeline(...);
	}
	partial PrName();
	public void Pr1()
	{
		PrName();
		Console.Writeline(group);
	}
}
```
### Анонимные типы
Как мы знаем при создании некоторого объекта, уже после этого можно создать экземпляр этого класса. Иногда есть необходимость иметь несколько разных объектов с каким-то набором атрибутов. Например, выше мы рассматривали объекты с полями Фамилия и группа, методы нас могут не интересовать, в таких случаях можно использовать анонимные типы, когда объект создается без описания класса
Описание может иметь вид:
```C#
var переменная = new { name1 = значение1, ..., nameN = значениеN;}
var st3 = new{name = "Петров", group = 2;}
Console.Writeline("Студент {0}, группа{1}", st3.name, st3.group);
st3.GetType();
```
Значение полей менять нельзя, нужно создать новый объект. Отметим, мы хотя бы класс не описываем, он автоматически генерируется.
## Дочерние классы. Наследование. Полиморфизм
### Наследие 
Создание дочерних классов является одним из основных инструментов ООП. Суть заключается в том, что на основе одних классов создаются другие. При этом исходный класс называется родительским или базовым, а другой дочерним 
Отметим, что все структуры создаются на основе ValueType, для них нет возможности создать дочерний класс. 
Если мы хотим запретить создавать дочерние классы для некоторого класса, мы должны добавить атрибут sealed.
дочерний класс наследует все члены родительского, если мы хотим, чтобы в методах дочернего использовались поля родительского, то в родительском для этих полей нужно использовать protected, а не private.

Пример:
```C#
class Student
{
	//объявление полей
	private string name;
	protected int mat;
	protected int fiz;
	protected int him;
	
	public string Name {get {return name}}
	public double SrBall() => (mat+fiz+him)/3.0;
	
	Student(string name, int mat, int fiz, int him)
	{
		this.name = name;
		this.mat = mat;
		this.fiz = fiz;
		this.him = him;
	}
}

//дочерний класс
class StudentIn: Student
{
	private int rus;
	
	StudentIn(string name, int mat, int fiz, int him, int rus)
	{
		this.name = name;
		this.mat = mat;
		this.fiz = fiz;
		this.him = him;
		this.rus = rus;
	}
}

class Program
{
	static void Main()
	{
	Student st1 = new Student("Иванов", 5, 4, 4);
	StudentIn st2 = new StudentIn("Боб", 5, 4, 4, 4);
	
	Console.Writline()
	}
}
```

### Полиморфизм
Из предыдущего примера мы видим, что методы дочерних классов в некоторых случаях желательно переопределить, такая возможность имеется, такое свойство называется полиморфизм. Можно в дочернем классе произвести новую реализацию метода, тогда в заголовке метода нужно добавить new. Добавим в дочерний класс метод
```C#
class Student
{
	//объявление полей
	private string name;
	protected int mat;
	protected int fiz;
	protected int him;
	
	public string Name {get {return name}}
	public double SrBall() => (mat+fiz+him)/3.0;
	
	Student(string name, int mat, int fiz, int him)
	{
		this.name = name;
		this.mat = mat;
		this.fiz = fiz;
		this.him = him;
	}
}

//дочерний класс
class StudentIn: Student
{
	private int rus;
	
	StudentIn(string name, int mat, int fiz, int him, int rus)
	{
		this.name = name;
		this.mat = mat;
		this.fiz = fiz;
		this.him = him;
		this.rus = rus;
	}
	public new SrBall() => (mat+fiz+him+rus)/4.0
}

class Program
{
	static void Main()
	{
	Student st1 = new Student("Иванов", 5, 4, 4);
	StudentIn st2 = new StudentIn("Боб", 5, 4, 4, 4);
	
	Console.Writline()
	}
}
```
## Запись экземпляра дочернего класса в переменную родительского типа. Виртуальные методы.
### Запись экземпляра дочернего класса в переменную родительского типа
Запись экземпляра дочернего класса в переменную родительский типа
Дочерний класс всегда является более узким, чем родительский, поэтому имеется возможность записать объекты созданные конструктором дочернего класса в переменную родительского класса обратной возможности нет.
Если к нему применить некоторый метод, то будет использован метод из родительского класса. Можно этот экземпляр переписать в переменную дочернего класса для этого следует использовать операцию к приведению типа
Например, так:
```C#
class Student
{
	//объявление полей
	private string name;
	protected int mat;
	protected int fiz;
	protected int him;
	
	public string Name {get {return name}}
	public double SrBall() => (mat+fiz+him)/3.0;
	
	Student(string name, int mat, int fiz, int him)
	{
		this.name = name;
		this.mat = mat;
		this.fiz = fiz;
		this.him = him;
	}
}

//дочерний класс
class StudentIn: Student
{
	private int rus;
	
	StudentIn(string name, int mat, int fiz, int him, int rus): base(name, mat, fiz, him)
	{
		this.rus = rus;
	}
	public new SrBall() => (mat+fiz+him+rus)/4.0
}

class Program
{
	static void Main()
	{
	Student st1 = new Student("Иванов", 5, 4, 4);
	StudentIn st2 = new StudentIn("Боб", 5, 4, 4, 4);
	
	Console.Writline()
	
	Student st3 = new StudentIn("Tim", 5, 3, 3, 5);
	StudentIn st4 = (StudentIn) st3;
	}
	
}
```
Мы могли привезти тип из того, что изначально объект был создан конструктором дочернего класса. Получили, что результат от зависит переменной(родительская или дочерняя), это может быть неудобным при решении некоторых задач. Уйти от этой зависимости можно используя виртуальные методы

### Виртуальные методы
В этом случае мы также имеем описание метода в родительском и дочернем. В родительском классе добавляется к методу атрибут virtual а в дочернем override
```C#
//родительский класс
class Student
{
	//объявление полей
	private string name;
	protected int mat;
	protected int fiz;
	protected int him;
	
	public string Name {get {return name}}
	public double SrBall() => (mat+fiz+him)/3.0;
	virtual public double SrBall1() => (mat+fiz=him)/
	
	Student(string name, int mat, int fiz, int him)
	{
		this.name = name;
		this.mat = mat;
		this.fiz = fiz;
		this.him = him;
	}
}

//дочерний класс
class StudentIn: Student
{
	private int rus;
	
	StudentIn(string name, int mat, int fiz, int him, int rus): base(name, mat, fiz, him)
	{
		this.rus = rus;
	}
	public new SrBall() => (mat+fiz+him+rus)/4.0;
	override public new SrBall() => (mat+fiz+him+rus)/4.0
}

class Program
{
	static void Main()
	{
	Student st1 = new Student("Иванов", 5, 4, 4);
	StudentIn st2 = new StudentIn("Боб", 5, 4, 4, 4);
	
	Console.Writline()
	
	Student st3 = new StudentIn("Tim", 5, 3, 3, 5);
	StudentIn st4 = (StudentIn) st3;
	}
	}
```
В обоих случаях будут использоваться реализация дочернего класса
## Класс System.Object
Все классы, в том числе и пользовательские, создаются на основе класса System.Object, то есть все они являются дочерними по отношению к нему, поэтому они наследуют члены данного класса. Класс System.Object содержит всего 4 метода, таким образом эти методы доступны всем классам
```C#
Eguls() //виртуальный метод, который сравнивает два экземпляра. Второй экземляр передается в виде поле. Если экземпляры находятся на одном участке памяти, то возращается True, если нет, то False
GetType() //возращает тип объекта, то есть название класса
GetHashCode() // возрвращает некоторый код соответствующий экземпляру, это виртуальный метод
ToString() //это виртуальный метод, возращает название класса
```
Данные методы виртуальные. В встроенных классах эти виртуальные методы переопределены. Например, если мы возьмем класс int
```C#
object st5 = new Student("Петров", 4, 5, 4);
object st6 = new Student("Петров", 4, 5, 4);
```
## Абстрактные классы.  Интерфейсы
### Абстрактные классы
Такие методы называются абстрактными и сам класс получается абстрактным. Абстрактные методы характеризуются тем, что у них отсутствует реализация. Приводится только заголовок. Абстрактные методы и классы оснащаются атрибутом abstract. Мы не может создать экземпляр абстрактного класса. Он может использоваться только как родительский при описании других классов. В значениях классов мы должны переопределить эти методы, то есть привести реализации, при этом используется атрибут override. Если все абстрактные методы родительские классы переопределены, то мы получаем обычный дочерний класс и можем создавать его экземпляры. Если же не все методы переопределены, мы опять получаем абстрактный класс, который может служить только родительским. Заметим, что при создании экземпляров дочернего класса можно использовать переменные родительского абстрактного класса. Предположим, мы хотим создать класс для работы с геометрическими фигурами и для нас важными являются значения площадей и периметр этих фигур. В общем случае нет формулы определения данных величин. Они существуют только для частных видов. Таким образом, опишем класс Figura с 2мя абстрактными методами, а затем создадим дочерние классы, в которых переопределим эти методы. 
Итак, имеем коды:

```C#
abstract class Figura
{
  public abstract 
    double Square();
  public abstract 
    double Perimeter();
}
```

```C#
class Triangle: Figure
{
  private double x,y,z;
  public Triangle(double x, double y, double z)
  {
    this x = x;
    this y = y;
    this z = z;
  }
  public override double Perimeter => x + y + z;
  public override double Square()
  {
    double P = (x + y + z)/2
    rerurn Math.Sqrt(p*(p-x)*(p-y)*(p-z))
  }
}
```

```C#
class Program 
{
  static void Main()
  {
    Figure f1 = new Triangle(3, 4, 5);
    Console.WriteLine("Площадь {0}, периметр {1}",f1.Square(), f1.Perimeter());
  }
}
```
Можно было в родительском классе вообще не указывать абстрактные методы, их ввести только в дочернем, но тогда, с одной стороны, глядя на родительский класс, мы вообще не увидели бы этих методов и не поняли бы связи между дочерними классами, а так мы видим, что во всех этих дочерних классах имеются методы для вычисления площадей и периметров. Другим важным моментом является то, что в дочерних классах для их использования, мы обязательно должны переопределить эти методы. Рассмотрим второй пример. Рассмотрим класс Human с одним полем и абстрактным методом вычисления зарплат. У этого класса будут два дочерних класса, которые отличаются принципом начислений зарплат, кроме абстрактного метода в классе Human опишем обычный метод, который будет выводить информацию о фамилии и зарплате, то есть в теле обычного метода будет использоваться абстрактный метод. 

```C#
abstract class Human
{
  protected string name;
  public abstract int Zarpl();
  public void Info() =>
    Console.WriteLine("Фамилия {0}, зарплата {1}", name, Zarpl());
}
```

```C#
class Worker: Human
{
  private int countDay;
  private int tarif;
  public override int ZarPl => CountDay * tarif;
  public Worker (string name, int CountDay, int tarif)
  {
    this.name = name;
    this.CountDay = CountDay;
    this.tarif = tarif;
  }
}
```

```C#
class Program 
{
  static void Main()
  {
    Human h1 = new Worker("Иванов", 21, 2000);
    h1.Info(); //Иванов, 42000
  }
}
```
### Интерфейсы
Интерфейс представляет собой дальнейшие обобщения абстрактного класса. Интерфейсы состоят только из абстрактных методов. При их описании используется ключевое слово interface. Чтобы получить шаблон, нужно также выбрать в меню "Добавить класс", а затем выбрать пункт "Интерфейс". Принято имена interface начинать с буквы I. При записи абстрактных методов модификатор доступа и слово abstract опускаются. Заметим, что интерфейсы могут выступать в качестве родительских и при создании структур. Важным отличием от наследования класса является то, что может быть несколько родительских интерфейсов. Более того, класс может быть дочерним для одного класса и нескольких интерфейсов. В этом случае они перечисляются через запятую. Первым записывается класс:
class Cl1: Cl2, In1, In2, ...
В отличии от наследования абстрактных классов при наследовании интерфейсов должны реализовываться все абстрактные методы. Интерфейсы также могут наследовать друг друга, добавляя новые абстрактные методы. 

```C#
interface IFigure
{
  double Square();
  double Perimeter();
}
```

## Использование классов для хранения данных.
Иногда удобно данные хранить в специальных классах, например, в классе Math хранятся значения математических констант. Рассмотрим варианты:
1. Класс без экземпляров. Суть: необходимые данные мы можем хранить в статических полях, поэтому мы сможем получать к ним доступ без создания экземпляра. Чтобы Экземпляр было невозможно создать, можно описать конструктор без параметров с модификатором private, то есть закрыть конструктор. В классах без экземпляров можно также описать статические методы, которые позволят выполнить определенные процедуры со статическими полями, например, мы можем тарифы или оклады работников хранить в статических полях, а затем при приеме на работу выбирать соответствующий член. Методы могут использоваться для индексирования зарплат. 
```C#
class Oklad
{
  private static int oklad1 = 50000;
  private static inr oklad2 = 60000;
  public static int Oklad1
  {
    return oklad1;
  }
  public static int Oklad2
  {
    return oklad2;
  }
  private Oklad()
  {
    
  }
  public static void Ind(double k)
  {
    oklad1 = (int)(oklad1 * k);
    oklad2 = (int)(oklad2 * k)
  }
}
```

```C#
static void Main()
{
  Ingener h1 = new Ingener("Иванов", ..., oklad.Oklad1);
  Oklad.Ind(1.25);
}
```

## Обобщения (хранение данных заранее неизвестного типа, обобщенные классы, использование нескольких параметров).
### Поля для хранения данных заранее неизвестного типа
Предположим, что мы хотим создать класс с 2мя полями. Первое поля для фамилии, второе поле для хранения некоторой информации. Заранее не знаем, какого типа будет информация. В зависимости от типа она может быть использована в разных преобразованиях. Возникает вопрос, какого типа поля описать. Можно использовать тип Object. Рассмотрим соответствующие коды:

```C#
class Person1
{
  private string name;
  private object info;
  public string Name
  {
    get
    {
      return name;
    }
    set
    {
      name = value;
    }
  }
  public object Info
  {
    set
    {
      return info
    }
    set 
    {
      info = value
    }
  }
  public Person1(string name, object info)
  {
    this.name = name;
    this.into = info;
  }
  public void P2 =>
  Console.WriteLine("Студент {0}, into{1}", name, info)
}
```
```C#
class Program
{
  static void Main()
}
Person1 pr1 = new Person1("Иванов", 5);
Person1 pr2 = new Person1("Петров", 5);
Person1 pr3 = new Person1("Сидоров", 'g');
Person1 pr4 = new Person1("Шишел-Мышел", true);
pr1.Pr();
pr2.Pr();
pr3.Pr();
pr4.Pr();
```
Экземпляры всегда создаются для выполнения некоторых преобразований. В качестве преобразования, мы будем использовать сложение для числового типа, отрицание для логического типа, сцепление для строковых.

```C#
pr1.Info = (int) pr1.Info + 1;
pr2.Info = (string) pr2.Info + 1;
pr3.Info = (char) pr3.Info + 1;
pr4.Info = !((bool) pr4.Info);
pr1.Pr(); // Иванов 6
pr2.Pr(); // Петров 51
pr3.Pr(); // Сидоров какое то число
pr4.Pr(); // Шишел-Мышел folse
```
Здесь неожиданно появляется 3 результат. В принципе, таким образом, мы можем добиться необходимых результатов. Однако здесь имеются проблемы. Тип int является значимый, тип object - ссылочный. Получается, что мы значимую величину упаковываем в ссылочный тип. При выполнении действий нужно выполнить распаковку. Если таких действий много, то они будут требовать больших ресурсов. Существует другая проблема, связанная с безопасностью. Предположим, что мы записали ошибочный оператор. Наш ход будет откомпелирован. И ошибка будет выявлена только на этапе выполнения, то есть возникнет исключение. Поэтому такой вариант использования типа objectсчитается нецелесообразным.

### Обобщенный класс
Обобщенные классы характеризуются тем, что в них для обозначения некоторого типа используется универсальный параметр. Этот параметр обычно обозначается прописной буквой и записывается в угловых скобках возле имени. При использовании класса выполняется типизация, то есть определяется какому типу соответствует данный параметр. Вместо параметра записывается этот тип. Заметим, что в одном и том же коде для разных экземпляров могут использоваться разные варианты типизации. Рассмотрим класс:

```C#
class Person2 <T>
{
  private string name;
  private T info;
  public string Name
  {
    get
    {
      return name;
    }
    set
    {
      name = value;
    }
  }
  public T Info
  {
    get
    {
      return info;
    }
    set
    {
      info = value;
    }
  }
  public Person2 (string name, T info)
  {
    this.name = name;
    this.info = info;
  }
  publuc void Pr() =>
  Console.WriteLine("Студент {0}, инфо{1}", name, info)
}
```

```C#
class Program
{
  static void Main()
  {
    Person2 <int> pr1 = new Person2 <int> ("Иванов", 5);
    Person2 <string> pr2 = new Person2 <string> ("Петров", "5");
    Person2 <char> pr3 = new Person2 <char> ("Сидоров", 'g');
    Person2 <bool> pr4 = new Person2 <bool> ("Шарапов", true);
    pr1.Pr();
    pr2.Pr();
    pr3.Pr();
    pr4.Pr();
    pr1.Info = pr1.Info + 1;
    pr2.Info = pr2.Info + 1;
    pr3.Info = (char)(pr3.Info + 1);
    pr4.Info = ! pr4.Info;
    pr1.Pr();
    pr2.Pr();
    pr3.Pr();
    pr4.Pr();
  }
}
```
### Обобщенный класс с несколькими параметрами
Обобщенный класс можно использовать несколько универсальных параметров. Они обозначаются разными буквами, через запятую и соответствуют разным типам данных 
```C#
class Person3<T, P>
{
	private string name;
	private T info;
	private P id;
	
	public string Name{...}
	public T Info {...}
	public P Id {}
	
	public Person3(string name, T info, P id)
	{
		this.name = name;
		this.info = info;
		this.id = id;
	}
	public void Pr() => Console.Writeline("Студент {0}, инфо {1}, id  {2}", name, info, id)
}


class Program
{
	static void Main()
	{
		Person3<int, string> pr1 = new Person3<int, string>("Иванов", 23, "A5")
	}
}
```

## Обобщения (использование обобщенного типа при типизации, обобщенные типы статических членов, обобщенные методы).
### Типизация при помощи обобщенного класса
В обобщенный класс входит универсальный параметр, при создании экземпляра класса, мы должны для этого параметра указать конкретный тип. Возникает вопрос, можно ли типизировать другим обобщенным классам? Единственное, для этого обобщенного  класса уде должна быть выполнена типизация, то есть мы должны вместо его параметров указывать уже конкретные типы.

Предположим у нас проводится соревнования создадим класс с двумя полями, название организации и участник из этой организации. Участник будет иметь тип Person2. Пока мы опишем класс, где тип участника запишем с помощью универсального параметра
Код:
```C#
class Games<T>
{
	private string name;
	private T member;
	
	public string Name {set {name = value;} get {return name;}}
	public T Member {set {member = value;} get {return member;}}
	
	public Games(string name, T membeer)
	{
		this.name = name;
		this.member = member;
	}
}

class Program
{
	static void Main()
	{
		Games<Person2<int>> g1 = new Games<Person2<int>>("КГАСУ", new Person2<int>("Иванов", 55));
		Console.Writeline("Вуз {0}, студент {1}, инфо {2}", g1.Name, g1.Member.Name, g1.Member.Info)
	}
}
```
### Использование обобщенного параметра в статических членах
Указанный в заголовке класса универсальный параметр, может использоваться и в статических членах, а именно в свойствах и методах. При описании нет отличия от динамических членов. Типизацию можно произвести при инициализации статического поля при помощи свойства. Мы можем выполнить целый ряд типизации для одного и того же поля. Таким образом, статического поле будет содержать целый спектр значений различных типов. Добавим в Person2 статические члены 
```C#
class Games<T>
{
	private string name;
	private T member;
	private static T code;
	
	public string Name {set {name = value;} get {return name;}}
	public T Member {set {member = value;} get {return member;}}
	public static T Code {set {code = value;} get {return code;}}
	
	public Games(string name, T membeer)
	{
		this.name = name;
		this.member = member;
	}
}


class Program
{
	static void Main()
	{
		Games<Person2<int>> g1 = new Games<Person2<int>>("КГАСУ", new Person2<int>                                 ("Иванов", 55));
		Console.Writeline("Вуз {0}, студент {1}, инфо {2}", g1.Name,                                         g1.Member.Name, g1.Member.Info)
		Person2<int>.Code = 7;
		Person2<string>.Code = "A3";
		Person2<char>.Code = 'f';
		Person2<bool>.Code = true;
		Console.Writeline("Для int {0}/n Для int {0}/n)
	}
}
```
### Обобщенные методы
Универсальный параметры могут использоваться для обозначения типов не только для описания класса, но и при описании метода. При этом типизация выполняется при вызове метода 

```C#
class Program
{
	static void F<T>(ref T x, ref T y)
	{
		T z = x;
		x  = y;
		y = z;
	}
	static void Main()
	{
		int a = 5;
		int b = 6;
		F(ref a, ref b);
		Console.Writeline("a = {0}, b = {1}", a, b);
		
		string u = ...
	}
}
```
## Наследование обобщенных классов
Рассмотрим вопрос о наследовании. У нас есть класс с универсальными параметрами. Рассмотрим дочерние классы, здесь возможны разные варианты, переберем их. Это мы произведем их. Это мы произведем на примере. В качестве родительского класса используем Person2. Дочерние классы будут иметь дополнительное поле, для которого также будет использоваться универсальный параметр. Дочерние классы будем называть PersonID с некоторым номером.
1. Дочерний класс может иметь тот же самый параметр:
```C#
class PersonId<T> : Person2<T>
{
	private T id;
	private T id { get {return id;}}
	private PersonId1(string name, T info, T id) : base (name, info)
	{
		this.id = id;
	}
}


class Program
{
	static void Main()
	{
		PersonId1<int> pr1 = new PersonId1<int>("Иванов", 53, 7);
	}
}
```

Новое поле вообще может иметь определенный тип
```C#
class PersonId<T> : Person2<T>
{
	private string id;
	private T id { get {return id;}}
	private PersonId1(string name, T info, T id) : base (name, info)
	{
		this.id = id;
	}
}


class Program
{
	static void Main()
	{
		PersonId1<int> pr1 = new PersonId1<int>("Иванов", 53, "A8");
	}
}
```

При описании дочернего класса мы уже можем выполнить типизацию родительского класса. Это указывается непосредственно в заголовке дочернего класса. Таким образом, мы получим, что дочерний класс является обычным классом, необобщенным
```C#
class PersonId<T> : Person2<string>
{
	private string id;
	private T id { get {return id;}}
	private PersonId1(string name, T info, T id) : base (name, info)
	{
		this.id = id;
	}
}


class Program
{
	static void Main()
	{
		PersonId1<int> pr1 = new PersonId1<int>("Иванов", 53, "A8");
	}
}
```
Можно типизировать родительский класс, а для добавить новый обобщенный параметр 
```C#
class PersonId4<T> : Person2<T>
{
	private T id;
	private T id { get {return id;}}
	private PersonId1(string name, T info, T id) : base (name, info)
	{
		this.id = id;
	}
}


class Program
{
	static void Main()
	{
		PersonId1<int> pr1 = new PersonId1<int>("Иванов", 53, "A8");
	}
}
```
## Ограничения обобщений
### 1. Необходимость ограничения 
Типизация универсального параметра можно использовать любой тип, иногда бывает удобно накладывать определенные ограничения. Вспомним, что при описании класса Person2 мы описали или включили, мы не могли записать аналогичный метод и вынуждены были вывод организjвать в классе Program

Ограничения позволяют описать внутри класса, которые в общем случае описать невозможно. Самым простым вариантом ограничением является некоторый класс. Дело в том, что в этом случае при типизации использовать не только его параметры, но и его дочерний класс. Ограничения указываются так:
```C#
class Game1<P> where P: Person2
{
	private string name;
	private P member;
	//свойства
	//конструктор
	public void Pr() => 
	Console.Writeline("Вуз {0}, студент{1}, инфо{2}", name, member.Name, member.Info)
}

static void Main()
{
	Game1<Person2<int>> g1 = new Game1<Person2<int>>("КГАСУ", Person2<int>("Иванов", 95));
	g1.Pr();
}
```

Например, так:
```C#
class Cl1<T, P>
{
	where T: Cl2
	where P: Cl3
}
```
### Стандартные ограничения
Если записать в качестве ограничения какой-то класс, то могут использоваться его дочерние классы
В первом случае можно будет типизировать данными ссылочного типа
Во втором случае значимого типа
В данном случае можно выполнить типизацию любым классом, у которого имеется открытый конструктор без параметров
Некоторый класс может содержать 
Если мы создаем дочерний класс с универсальным параметром, этот параметр также находится в родительском классе и на него есть ограничения. Причем не слабее, чем в родительском. Например, могут быть 
```C#
class Cl1<T> where T: class
{
	тело
}
class Cl2<T>: Cl1<T> where T: Person1
{
	тело
}
```
Ограничения полноценных классов. В воплащенных методах, мы также можем указывать ограничения 
```C#
static void G<T>(T x) where T: Person2
{
	Console.Writeline(x.Name);
}
static void Fcl<T>(ref T x, ref T y) where T: class
{
	T z = x;
	x = y;
	y = z;
}
```
Рассмотрим стандартное ограничение для методов:
  ```C#
  class Game1<P, T> where P: Person2<T>
  ```
## Индексаторы (описание индексаторов, перегрузка индексаторов, индексирование компонентов полей, использование нескольких индексов)
### Описание Индексаторов
Предположим класс содержит некоторое количество однотипных полей, иногда бывает удобно обращаться к полям не по имени, а по некоторому индексу. Рассмотрим, как это реализовать. Для этой цели используются индексаторы, они имеют следующий вид:
```C#
тип this[параметр]
{
	set{...}
	get{...}
}
```
Тип определяет индексированного объекта. Если мы индексируем поля типа int, это будет int. Параметры - некоторые параметры с указанием типа. Если их несколько, то указываем через запятую. set и get предназначены для установки значения и получения значения некоторого поля. 
Пример:
```C#
class Student
{
	public string Name{set; get;}
	public int Group{set; get}
	public int Mat{set; get}
	public int Fiz{set; get}
	public int Him{set; get}
	
	public double SrBall()
	{
		return (Mat + Fiz + Him) / 3.0
	}
	
	public Student(string name, int group, int mat, int fiz, int him)
	{
		Name = name;
		Group = group;
		Mat = mat;
		Fiz = fiz;
		Him = him;
	}
	
	public int this[int k]
	{
		set
		{
			switch(k)
			{
				case 0:
					Mat = value;
					break;
				case 1:
					Fiz = value;
					break;
				case 2:
					Him = value;
					break;
				default: throw new Exeption("Неправильный индекс!");
			}
		}
		get
		{
			switch(k)
			{
				case 0:
					return Mat;
				case 1:
					return Fiz;
				case 2:
					return Him;
				default: throw new Exeption("Неправильный индекс!");
			}
		}
	}
}

class Program
{
	{
		static void Main()
		{
			Student st1 = new Student("Иванов", 51, 5, 4, 4);
			st1[0] = 5;
			Console.Writeline("Student {0}, math {1}", st1.Name, st1.Mat);
		}
	}
}
```
### Перегрузка индексатора 
Индексаторы по аналогии с методами можно перегружать. Таким образом, могут одновременно быть описаны несколько индексаторов для одних и тех же объектов. Они должны отличаться типом, количеством или последовательностью индекса. Определив индексатор, где индекс будет иметь тип string.
Пример:
```C#
public int this [string s]
{
	set
	{
		switch(s)
		{
			case "Математика" : Mat = value; break;
			case "Физика" : Fiz = value; break;
			case "Химия" : Him = value; break;
			default: throw new Exeption("Неправильный индекс!");
		}	
	}
	get
		{
			switch(k)
			{
				case "Математика":
					return Mat;
				case "Физика":
					return Fiz;
				case "Химия":
					return Him;
				default: throw new Exeption("Неправильный индекс!");
			}
		}
}
```
### Индексирование компонентов поля 
Выше мы индексировали поля и индекс соответствовал некоторому полю. Поля могут иметь сложную конструкцию, например, быть массивами. Можно описать индексатор, чтобы индекс соответствовал компонентам этого поля. Для этого опишем класс Students. У него будет одно поле, которое представляет собой массив студентов. Опишем индексаторы для доступа к элементам этого поля. Получим код:

```C#
class Students
{
  private Student[] stud;
  public Students(Student[] stud)
  {
    this.stud = stud;
  }
  public Student this[int k]
  {
    set{stud[k] = value;}
    get{return stud[k];}
  }
}
```
```C#
class Program
{
  static void Main()
  {
    student st2 = new Student("Петров", .....);
    student st3 = new Student("Сидоров", .....);
    student st4 = new Student("Попов", .....);
    student st5 = new Student("Алексеев", .....);
    Student[] sts = {st1, st2, st3, st4, st5};
    Students studs = new Students[sts];
    Console.WriteLine("Студент{0}, ср.балл{1}", studs{2}.Fan, studs{2}.SrBall; Сидоров .....);
  }
}

```
Предположим мы хотим сосчитать общий балл у студентов по математике
```C#
int n = sts.Lenght;
double s = 0;
for (int k = 0; k < n; k++)
{
	s+= studs(k)[0];
	double sz_mat = s/n;
}
```

Выше мы индекс никак не связывали с содержимым самого объекта, можно в качестве индекса использовать какую-то компоненту. Например, можно использовать фамилию студента. Опишем в классе студент индексатор, типа стринг только для чтения, чтобы получать студента по его фамилии
Код может иметь вид:
```C#
public Student this[string s]
{
	get 
	{
		foreach(Student a in stud)
		{
			if (a.Name == s)
				return a;
			throw new Exeption("Такого студента нет!");
		}
	}
}

class Program
{
	static void Main()
	{
		Console.Writeline("Student {0}, group {1}", studs["Иванов"].Name, studs["Иванов"].Group);
	}
}
```

### Использование нескольких индексов 
Индексаторы могут содержать несколько индексов. Они пишутся через запятую
```C#
class Matrix
{
	private int m = new int[,]{{1, 2, 3}, {4, 5, 6}};
	public int this[int i, int j]
	{
		set{ m[i, j] = value;}
		get{ return m[i, j];}
	}
}

class Program
{
	static void Main()
	{
		Matrix mt = new Matrix();
		mt[1, 2] = 10;
		Console.Writeline("First{0}, last{1}", int [0, 0], int [4, 2])
	}
}
```
## Коллекции
### Коллекция List`<T>
Обычно в языках программирования с однотипными данными используют массивы, но в некоторых случаях используют коллекции. Они обладают  соответствующими свойствами и методами и эффективно обрабатывать эти объекты, также отметим, что в коллекции есть возможность добавление членов и удаление
Рассмотрим основные виды коллекции:
Коллекция List соответствует простому списку. Создать коллекцию можно при помощи конструктора на основе некоторого массива или конкретных объектов. Типизация определяет тип объектов, входящих в коллекцию. Мы в качестве примера будем рассматривать экземпляры класса студент. Предположим, что этот класс имеет поля - mat name, fiz, him. Соответствующие методы для ввода и вывода и для вычисления среднего балла и конструктор с параметрами. Отметим, что м

```C# 
Student st1 = new Student("Иввнов", 5, 4, 5);

Student[] stnd = {st1, st2};
List<Student> l1 = new List<Student>(stnd){st3};
Console.Writeline(l1[1].Name); // Петров

l1.Add(st4); //st1, st2, st3,st4
l1.Insert(2, st5); // st1, st2, st5, st3, st4
l1.Insert(4, st1); // st1, st2, st5, st3, st1, st4

foreach (Student x in l1)
	Console.Writeline("студент {0}, средний балл {1}", x.Name, x.SrBall());
	
int k = l1.Count;
for (int i = 0, i < k, i ++)
{
	Console.Writeline("Студент {0}, ср {1}", l1[i].Name, l1[i].SrBall());
}

RemoveAt(1); // удаляет по индексу
Remove(st5) // удаляет по названию

Contains() //если объект в данном массиве

int n = l1.IndexOf(st1); // 0
int m = l1.LastIndexOf(st1); // 2

l1 = l1.Reverse(); // меняет индексы наоборот
l1 = l1.Clear(); // удаляет всю коллекцию
```

### Коллекция Queue`<T>
Соответствует очереди, которая соответствует FIFO. В этом случае очередной член при добавлении встает на конец очереди, извлекаться члены могут только с начала очереди. Можно создать очередь на основе некоторого массива или другой коллекции. Здесь нет номера, мы не можем работать for только с foreach. Для добавления используется метод Push(). Как мы указали члены могут извлекаться только в начале - это функция Pop(), которая возвращает первый член и убирает из очереди. Peek() показывает, но не удаляет.

```C#
Student st1 = new Student("Иввнов", 5, 4, 5);

Student[] stnd = {st1, ..., st4};
Queue<Student> q1 = new Queue<Student>(st);
Console.Writeline(l1[1].Name); // Петров

q1.Enqueue(st5); // вывод колекции
Degqueue();
Peek();

foreach(Student x in q1)
{
	Console.Writeline("студент {0}, средний балл {1}", x.Name, x.SrBall());
}

while (q1.Count != 0)
{
	Student x = q1.Dequeue();
	Console.Writeline("Фамилия {0}, ср балл {1}", x.Name, x.SrBall())
}
```

### Коллекция Stack`<T>
Аналог очереди работает по аналогии LIFO. Очередной объект размещается в начале очереди, кто последний заходит, тот первым выходит, методы аналогичны последней коллекции
```C#
Student st1 = new Student("Иввнов", 5, 4, 5);

Student[] stnd = {st1, ..., st4};
Stack<Student> q1 = new Stack<Student>(st);
Console.Writeline(l1[1].Name); // Петров

q1.Push(st5); // вывод колекции
Pop();
Peek();

foreach(student x in s1)
{
	Console.Writeline("студент {0}, средний балл {1}", x.Name, x.SrBall());
}
```
### Коллекция Dictionary<K, L>
Это коллекция соответствует словарям. В данном случае наш объект, входящий в коллекцию состоит из ключа и объекта, в данном случае эта коллекция аналогична списку. В списке мы могли получить доступ по номеру, здесь по ключу. Номер является целым пол числом, он может изменяться при удалении, вставке и т.д. Ключ же может принимать любое значение и не меняется ни при каких действиях. Таким образом, во время создания экземпляра нужно сделать по значению и по ключу, можно создать экземпляр словаря по аналогии создания двумерного массива в качестве значений мы будем использовать экземпляры класса студент, а в качестве ключа название вуза
```C#
Student st1 = new Student("Иввнов", 5, 4, 5);

Student[] stnd = {st1, ..., st4};
Dictionary<string, Student> d1 = new Dictionary<string, Student>
{
	["КГАСУ"] = st1,
	["КФУ"] = st2,
	["КЮИ"] = st3 
}

d1.Add("МГПУ", st4); //Добавление нового элемента
d1.Remove("КЮИ"); //Удаление по ключу

foreach(var x in d1)
{
	Console.Writeline("Student {0}, for {1}", x.Value.Name, x.Key)
}

Console.Writeline(d1.ContainsValue(st5)));

Console.Writeline(d1.ContainsKey("КГАСУ"));
```
При помощи метода Add() можно добавить объект с двумя значениями. 
Можно проверить наличие конкретного объекта или конкретного ключа, для этого используется соответствует методы ContainsValue или ContainsKey()
## Понятие делегата. Обобщенные делегаты(не нашел) и встроенные делегаты
### Понятие делегатов
Мы описывали классы разных типов и каждый класс определяет, делегат это тоже некоторый аналог класса он определяет функциональный тип значения его экземпляров являются разные методы, делегат именно определяет какой сигнатурой могут является его значения, заметим, что слово делегат здесь применяется в двух смыслах, как сам класс и как его экземпляр 
delegate тип имя (перемены);
```C#
delegate void Del();
```
Теперь нам надо описать методы соответствующих типов
Распишем 3 метода 
1. Статический - запишем в классе Program
2. Статист

```C#
namespace Project1
{
	delegate void Del();
	Class Test
	{
		public void F1() => Console.Writeline("Динамический");
		public static void F2() => Console.Writeline("Статический");
	}
	class Program
	{
		static void F3() => Console.Writeline("Вищественный");
		static void Main()
		{
			Del d2 = new Del(F3);
			d1();
			d1 = Test.F2;
			d1();
			d1 = new Test().F1;
			d1();
		}
	}
}
```
Естественно делегаты могут иметь какие-то параметры и могут возвращать какие-то значения. Его описание может иметь вид:
```C#
namespace Project1
{
	delegate int Fun(int, x, int y);
	class Program
	{
		int G1(int x, int y) => x+y;
		int G1(int x, int y) => x*y;
		Pr<int, int, int> p1 = G1;
		delegate T Pr<K, P, T>(K x, P y)
		Fun fun1 = G1;
		Console.Writeline(G1(3,4));
		fun1 = G2;
		Conslole.Writeline(G1(3, 4));
	}
}
```

### Встроенные делегаты 
Имеются делегаты, которые встроенные в фреймворк, рассмотрим некоторые из них
1. Делегат Action<T1, T2, ...> - это обобщенный делегат, у него до 16 параметров они определяют типы входных параметров. Этот делегат ничего не возвращает. Таким образом, описание данного делегата - выглядит так: delegate void Action<T1, t2>(T1 x1, T2 x2, ...)
2. Delegate Func<T1, T2, ...> - здесь также 16 параметров, последний определяет, выходящее значение, остальные - параметры
3. Делегат Predictect`<T>` - имеет один входной параметр и возвращает bool 
## Добавление методов в делегат и их удаление(не нашел).  Анонимные методы. Лямбда-выражения
### Анонимные методы и лямбда-выражения 
Иногда методы используются только один раз, например, при инициализации делегатов, в таких случаях нет необходимости делать или описывать отдельный метод можно соответствующие тело прописать непосредственно в коде, использующий этот метод. Описанные таким образом методы называются - анонимные
```C#
delegate(параметры)
{
	body
}

Action del5 = delegate 
				{
					Console.Writeline("Анонимный метод");
				};
```
Как мы видим, если параметр отсутствует, то его можно опустить
```C#
Func <int, int, int> dues = delegate(int x, int y) {return x - y;};
```
Рассмотрим лямбда выражения. Если тело в одну строку, то не пишут фиг скобки, если только return, то не пишем return 
```C#
параметры => тело

//пример
Action del7 = () => 
{
	Console.Writeline("Лямбда");
	Console.Writeline("выражение");
}

Func <int, int, int>  del8 = (x, y) => x-y;
```
Анонимные методы и лямбда выражения могут использоваться в делегатов на ровне со всеми. Один и тот же делегат может одновременно указывать на несколько методов. В начале делегат инициализируется обычным образом, потом другим методы добавляются при помощи += в результате другие методы прописываются в списке вызова данного делегата и при выполнении делегата выполняются все методы из этого списка
```C#
Action del9 = new Test.F1;
	del9 += Test.F2;
	del9+= F3;
	del9 += new Test().F1;
	del9 += delegate{Console.Writeline("Анонимный");};
	del9 += () => Console.Writeline("Лямбда");
	del9();
	
Action del10 = new Test().F1;
Action del11 = Test.F2;
Action del12 = F3;
Action del13 = del10 + del11 + del12;
del13();
```
Если указанный метод есть в списке, то он удаляется, если нету, то операция игнорируется, если два, то удаляется тот, что ближе к концу
Иногда при удалении методов возникает опасность, что мы удалим все методы и получим значение Null - пустой, при попытке выполнить этот делегат получим ошибку, поэтому после выполнения желательно проверить, что мы не имеем значение null

Рассмотрим добавление функций, когда соответствующие методы что-то возвращают, в этом случае тоже все функции все включаются из списка, только отличие заключается в том, что выполнzются тела этих функций за исключением оператора return, этот оператор выполняется только для последней функции 
Рассмотрим пример:
```C#
Func <int, int, int> del14 = G1;
	del14 += G2;
	int a = del14(3, 8);
Console.Writeline(a.ToString());

int G1(int x, int y)
{
	Console.Writeline("Сумма");
	return x + y;
}

static int G2(int x, int y)
{
	Console.Writeline("Произведение");
	return x * y;
}
```
Мы получаем только return
## Использование делегатов при описании метода
Рассмотрим использование делегатов при описании полей класса. Представим, что мы создаем определенный класс и в его коде мы генерируем сообщение и оно отправляется пользователю, мы знаем, что это сообщение. но мы не знаем на какие устройства нужно их отправлять, например, рассмотрим класс, работающий с расчетом в некотором банке, клиент может совершать какие-то действия. Пользователь может снимать и класть деньги при этом он должен получать соответствующие сообщение, при чем у каждого пользователя свои предпочтения куда его отправлять, поэтому разработчик долен создать универсальный класс, который подходит для всех пользователей, он может для этого использовать делегаты. При создании делегатов, этим делегатом в качестве значений могут присваиваться определенные методы

Для примера сначала мы рассмотрим класс без использования делегатов
В нашем классе предполагаются поля для фамилии суммы и пароля
Методы - пополнение счета и снятия, при каждом этом действие должно быть сообщение. Также опишем метод для организации сеанса
```C#
class Account4
{
	private Action<string> sob;
	private string name;
	private int sum;
	private string password;
	
	public Account4(string name, int sum, string password, Action<string> sob)
	{
		this.name = name;
		this.sum = sum;
		this.password = password;
		this.sob = sob;
	}
	
	void Add(int s)
	{
		sum += s;
		string str = String.Format("Клиент {0}, пополнен: {1}", name, sum);
		Console.Writeline(str);
	}
	void Sub(int s)
	{
		if (s <= sum)
		{
			sum -= s;
			string str = String.Format("Клиент {0}, снято: {1}", name, sum);
			Console.Writeline(str);
		}
		else
		{
			string str = String.Format("Клиент {0}, средств недостаточно {1}", name, sum);
			Console.Writeline(str);
		}
	}
	public void Act()
	{
		Console.Writeline("Клиент {0}, введите пароль!", name);
		string pw = Console.Readline();
		string code = "0"
		if  (pw != password)
		{
			Console.Writeline("Неправильный пароль!");
		}
		else
		{
			do 
			{
				Console.Writeline("Введите код операции:\n
								   0 - выход;\n
								   1 -  пополнить\n
								   2 - снять");
				string code = Console.Readline();
				int s;
				if (code == 1 or == 2)
				{
					Console.Writeline("Введите сумму!")
					s = Int32.Parse(Console.Writeline())
				}
				if (code == s)
				{
					Add(s);
				}
				if (code == 2)
				{
					Sub(s);
				}
				while (code != 0);
			}
		}
	}
}

class Program
{
	static void PrCon(string z) => Console.Writeline(z);
	static void PrFile1(string z)
	{
		StreamWrites sw = new StreamWrites(@"..", true, Encoding.Default);
		sw.Writeline(z);
		sw.Close(); 
	}
	
	static void Main()
	{
		Action<sting> del1 = PrCon;
		del1 += PrFile1;
		Account4 acc7 = new Account4("Иванов", 50, "123", del1);
		acc7.Act();
		del1 -= PrFile1;
		del1 += prFile2;
		Account4 acc8 = new Account4("Иванов", 50, "123", del1);
		acc8.Act();
	}
}
```
## Поля класса типа делегат. Варианты инициализации (не нашел)

## События. Делегат EventHandler
В классе аккаунт6 мы для обработки события мы использовали делегат `Action<strung>` в принципе получили рабочую программу, но желательно для обработки событий использовать делегат EventHandler - обработчик событий. Этот делегат имеет два параметра:

```C#
EventHandler(object a, EventArgs e)
```
а - объект, который вызвал событие. е - контейнер, пустой класс его нужно создать дочерний, который содержит информацию о данном событии, чем вызвано событие 
Рассмотрим код:
таким образом мы должны описать дочерний класс, таким образом мы можем создать поля для старого и нового значения сумму при совершении события 
```C#
class AccountEventArgs: EventArgs
{
	private int oldSum;
	private int newSum;
	
	public int OldSum { get {return oldSum} }
	public int NewSum { get {return newSum} }
	
	public AccontEventHandler(int oldSum, int newSum)
	{
		this.oldSum = oldSum;
		this.newSum = newSum;
	}
}

class Account7
{
	name, sum, password //теже поля
	private EventHandler sob;
	
	//конструктор остается темжу
	
	public string Name {get {return name;}}
	public event EventHandler sob {//остается}
	public void act() {//остается}
	public void Add(int s)
	{
		int oldSum = sum;
		sum += s;
		sob(this, new AccontEventHandler(oldSum, sum));	
	}
	public void Sub(int s)
}

class Program
{
	void PrCon(object a, EventArgs e)
	{
		string name = ((account7)a).Name;
		int oldSum = ((AccountEventAgrs)e).OldSum;
		int newSum = ((AccountEventAgrs)e).NewSum;
		string
	}
}
```

# Практика
## Задание 1
Задача на описание и использование классов и структур, при этом необходимо уметь описывать дочерние классы, частичные классы и методы, использовать анонимный тип, абстрактные классы и интерфейсы, а также классы для хранения данных, создавать и использовать массивы пользовательских объектов.
### Класс student. Поля фамилия и стипендия. Свойства для чтения. Конструктор с параметрами для инициализации. Три значения стипендии в виде перечислений. В мейне создать объект студента и вывести о нем инфу.
```C#
    class Program
    {
        static void Main(string[] args)
        {
            Student st = new Student("Иван", Step.Medium);
            Console.WriteLine($"Фамилия: {st.Name}");
            Console.WriteLine($"Степендия: {(int)st.Step}");
        }
    }

    enum Step
    {
        High = 5000,
        Medium = 3000,
        Low = 1000
    }
    
    class Student
    {

        private string name;
        private Step step;
        public string Name

        {   get { return name; }
            set { name = value; }
        }

        public Step Step
        {
            get { return step; }
            set { step = value; }
        }

        public Student(string name, Step step)
        {
            this.name = name;
            this.step = step;
        }
    }
```
### Создать класс студент с полями фамилия и стипендия свойства для этих полей в майне описать вывести на консоль студента и стипендию стипендия через 3 константы выводить
```C#
class Program
    {
        static void Main(string[] args)
        {
            Student st1 = new Student("Иван", Student.High);
            Console.WriteLine($"Фамилия {st1.Name} {st1.Step}");
        }
    }

    class Student
    {
        private string name;
        private int step;
        public const int High = 5000;
        public const int Medium = 3000;
        public const int Low = 1000;
        
        public string Name
        {
            get { return name; }
            set { name = value; }
        }

        public int Step
        {
            get { return step; }
            set { step = value; }
        }

        public Student(string name, int step)
        {
            this.name = name;
            this.step = step;
        }
    }
```
### Создать класс Human: Поля фамилия и стипендия, свойства на ввод/вывод. Дочерний от него класс Student: Метод на изменение стипендии на 100. B Main() создать экземпляр, инициализировать поля, вызвать этот метод и вывести фамилию и стипендию
```C#
class Program
    {
        static void Main(string[] args)
        {
            Student st = new Student("Ivan", 10000);
            st.Stepa();
            Console.WriteLine($"{st.Name} {st.Step}");
        }
    }

    class Human
    {
        private string name;
        private int step;
        
        public string Name
        {
            get { return name; }
            set { name = value; }
        }

        public int Step
        {
            get { return step; }
            set { step = value; }
        }
        
        public Human(string name, int step)
        {
            this.name = name;
            this.step = step;
        }
    }

    class Student:Human
    {
        public Student(string name, int step) : base(name, step)
        { }

        public void Stepa()
        {  Step += 100;   }
    }
```
### создать класс студент описать поля имя и стипа описать свойства для вывода описать метод для увеличения стипы на 100. вывести экземпляр имя и используя метод фамилию
```C#
class Program
    {
        static void Main(string[] args)
        {
            Student st1 = new Student("A", 1000);
            st1.Stepa();
            Console.WriteLine($"Имя {st1.Name} {st1.Step}");
        }
    }

    class Student
    {
        private string name;
        private int step;

        public string Name
        {
            get { return name; }
            set { name = value; }
        }

        public int Step
        {
            get { return step; }
            set { step = value; }
        }

        public Student(string name, int step)
        {
            this.name = name;
            this.step = step;
        }

        public void Stepa()
        {   step += 100;   }
    }
```
### Создать класс с полями имя и стипендия. Свойства - вывод полей. Создать перечисления с 3 значениями по поводу стипендии. Вывести на консоль один экземпляр.
```C#
class Program
    {
        static void Main(string[] args)
        {
            Student st1 = new Student("Иван", Stepa.h);
            Console.WriteLine($"{st1.Step} {(int)st1.Step}");
        }
    }

    enum Stepa
    {
        h = 5000,
        m = 3000,
        l = 1000
    }

    class Student
    {
        private string name;
        private Stepa step;

        public string Name
        {
            get { return name; }
            set { name = value; }
        }

        public Stepa Step
        {
            get { return step; }
            set { step = value; }
        }

        public Student(string name, Stepa step)
        {
            this.name = name;
            this.step = step;
        }
    }
}
```
### Описать студента. Поля фамилия и стипендия. Свойства для ввода и вывода данных. B Main вывести данные о студенте. Стипендия должна быть 3 варианта
```C#
class Program
    {
        static void Main(string[] args)
        {
            Student st1 = new Student("Иван", Stepa.h);
            Console.WriteLine($"{st1.Step} {(int)st1.Step}");
        }
    }

    enum Stepa
    {
        h = 5000,
        m = 3000,
        l = 1000
    }

    class Student
    {
        private string name;
        private Stepa step;

        public string Name
        {
            get { return name; }
            set { name = value; }
        }

        public Stepa Step
        {
            get { return step; }
            set { step = value; }
        }

        public Student(string name, Stepa step)
        {
            this.name = name;
            this.step = step;
        }
    }
```
### абстрактный класс human с полем фамилия и абстрактным методом Stepa, свойства ввода/вывода. Дочерний класс student с полем стипендии и свойства ввода и вывода, сделать стипендию на 100 больше в main()
```C#
   class Program
    {
        static void Main(string[] args)
        {
            Student st1 = new Student();
            st1.Name = "Ivan";
            st1.Step = 5000;
            st1.Stepa();
            Console.WriteLine($"{st1.Name} {st1.Step}");
        }
    }

    abstract class Human
    {
        private string name;
        
        public string Name
        {
            get { return name; }
            set { name = value; }
        }

        public abstract void Stepa();
    }

    class Student : Human
    {
        private int step;
        
        public int Step
        {
            get { return step; }
            set { step = value; }
        }

        public override void Stepa()
        {   step += 100;  }
    }
```
### В одном Student (фамилия, стипа).Реализовать ввод /вывод. Стипу записать в классе без экземпляра (3 раза)
```C#
public class Student
{
    public string LastName { get; set; } // Фамилия студента
    public static int Stip { get; private set; } // Стипендия (статическое поле)

    // Метод для записи стипендии (статический)
    public static void SetStip(int amount)
    {
        Stip = amount;
        Console.WriteLine($"Стипендия установлена: {Stip} рублей");
    }

    // Конструктор для создания экземпляра
    public Student(string lastName)
    {
        LastName = lastName;
    }

    // Метод для вывода информации о студенте
    public void DisplayInfo()
    {
        Console.WriteLine($"Фамилия: {LastName}, Стипендия: {Stip} рублей");
    }
}

class Program
{
    static void Main()
    {
        // Установка стипендии без создания экземпляра класса
        Student.SetStip(1500);
        Student.SetStip(2000);
        Student.SetStip(2500);

        // Создание экземпляров студентов
        Student student1 = new Student("Ivanov");
        Student student2 = new Student("Petrov");

        // Вывод информации о студентах
        student1.DisplayInfo(); // Выводит: Фамилия: Ivanov, Стипендия: 2500 рублей
        student2.DisplayInfo(); // Выводит: Фамилия: Petrov, Стипендия: 2500 рублей
    }
}
```
### Класс student имеет два поля (фамилию/стипендию)+конструктор инициализирующий эти поля. И два свойства для извлечения этих полей. Класс реализует интерфейс, а метод должен увеличивать стипу на 100
```C#
class Program
    {
        static void Main(string[] args)
        {
            Student st1 = new Student("Ivan", 2000);
            st1.Stepa();
            Console.WriteLine($"{st1.Name} {st1.Step}");
        }
    }
    interface Human
    {
        void Stepa();
    }
    class Student: Human
    {
        private string name;
        private int step;
        public string Name
        {
            get { return name; }
            set { name = value; }
        }
        public int Step
        {
            get { return step; }
            set { step = value; }
        }
        public Student(string name, int step)
        {
            this.name = name;
            this.step = step;
        }
        public void Stepa()
        {
            step += 100;
        }
    }
```
### Интерфейс Human с членами: метод Stepa. Дочерний класс Student с членами: поля - фамилия, стипендия; свойства - для ввода-вывода значений полей; метод Stepa изменяет стипендию на 100. Main - создать экземпляр, инициализировать поля, применить метод, вывести на консоль фамилию и стипендию.
```C#
class Program
    {
        static void Main(string[] args)
        {
            Student st = new Student("Иванов", 3500);
            st.Stepa();
            st.Info();
        }
    }
    interface Human
    {
        void Stepa();
    }
    class Student : Human
    {
        private string name;
        private int step;
        private string Name
        {
            get { return name; }
            set { name = value; }
        }
        private int Step
        {
            get { return step; }
            set { step = value; }
        }
        public Student(string name, int step)
        {
            this.name = name;
            this.step = step;
        }
        public void Stepa()
        {step += 100; }
        public void Info()
        {
            Console.WriteLine($"Фамилия: {Name}");
            Console.WriteLine($"Степендия: {Step}");
        }
    }
}
```
### 2.	интерфейс human с методом Stepa (). Класс student имеет два поля (фамилию/стипендию) +конструктор инициализирующий эти поля. И два свойства для извлечения этих полей. Класс реализует интерфейс, а метод должен увеличивать стипу на 100
```C#
// Интерфейс Human
public interface IHuman
{
    void Step();
}

// Класс Student реализует интерфейс Human
public class Student : IHuman
{
    // Поля класса
    private string surname;
    private int stipend;

    // Конструктор, инициализирующий поля
    public Student(string surname, int stipend)
    {
        this.surname = surname;
        this.stipend = stipend;
    }

    // Свойства для извлечения значений полей
    public string Surname
    {
        get { return surname; }
    }

    public int Stipend
    {
        get { return stipend; }
    }

    // Реализация метода Step интерфейса IHuman
    public void Step()
    {
        stipend += 100;
        Console.WriteLine($"Stipend increased! New stipend: {stipend}");
    }
}

class Program
{
    static void Main(string[] args)
    {
        // Создание экземпляра Student
        Student student = new Student("Ivanov", 5000);

        // Вывод начальной информации
        Console.WriteLine($"Surname: {student.Surname}");
        Console.WriteLine($"Initial Stipend: {student.Stipend}");

        // Вызов метода Step
        student.Step();

        // Вывод обновленной информации
        Console.WriteLine($"Updated Stipend: {student.Stipend}");
    }
}

```
## Задание 2
Задача, для решения которой необходимо уметь
●      описывать и использовать обобщенный класс;
●      описывать и использовать обобщенный метод;
●      описывать и использовать дочерний обобщенный класс;
●      использовать стандартные ограничения обобщений;
●      описывать и использовать индексаторы полей;
●      создавать все типы коллекций и использовать их;
●      описывать делегаты, добавлять в них методы,  анонимные методы, лямбда-выражения, исключать их, а также выполнять делегаты.
### Делегат, который ничего не возвращает, без параметров. Добавить к нему анонимный метод, который выводит вашу фамилию. Добавить лямбда выражение, которое выводит имя. Выполнить делегат
```C#
class Program
{
    // Определяем делегат без параметров и возвращаемого значения
    public delegate void MyDelegate();
    static void Main()
    {

        // Создаем делегат
        MyDelegate myDelegate;
        // Добавляем анонимный метод, который выводит фамилию
        myDelegate = delegate
        {
            Console.WriteLine("Фамилия: Иванов");
        };

        // Добавляем лямбда-выражение, которое выводит имя
        myDelegate += () => Console.WriteLine("Имя: Иван");
        myDelegate();}
```
### Создать класс Student, с полями фамилия и Степа и свойствами на вывод, создать конструктор . В маине создать 5 экземпляров и создать словарь на основе 4, добавить 5 в словарь и вывести имя и фамилию студента по очереди крч (цикл используется)
```C#
{
        static void Main()
        {
            Student student1 = new Student("Иванов", 1000);
            Student student2 = new Student("Петров", 1200);
            Student student3 = new Student("Сидоров", 1500);
            Student student4 = new Student("Кузнецов", 1300);
            Student student5 = new Student("Смирнов", 1400);
            
            Dictionary<string, Student> st = new Dictionary<string, Student>
            {
                {student1.Name, student1 },
                {student2.Name, student2 },
                {student3.Name, student3 },
                {student4.Name, student4 },
            };

            st.Add(student5.Name, student5);
            foreach (var entry in st)
            { Console.WriteLine($"Фамилия: {entry.Value.Name}, Стипендия: {entry.Value.Step}"); }
        }
    }

    class Student
    {
        private string name;
        private int step;
        
        public string Name
        {
            get { return name; } set { name = value; }
        }
        
        public int Step
        {
            get { return step; } set { step = value; }
        }
        
        public Student(string name, int step)
        {
            this.name = name; this.step = step;
        }
    }
```
### Cоздать стек какой то там
```C#
class Program
    {
        static void Main()
        {
            Student student1 = new Student("Иванов", 1000);
            Student student2 = new Student("Петров", 1200);
            Student student3 = new Student("Сидоров", 1500);
            Student student4 = new Student("Кузнецов", 1300);
            Student student5 = new Student("Смирнов", 1400);

            Stack<Student> st = new Stack<Student>();
            st.Push(student1);
            st.Push(student2);
            st.Push(student3);
            st.Push(student4);
            st.Push(student5);

            while (st.Count > 0)
            {
                Student st1 = st.Pop();
                Console.WriteLine($" {st1.Name} - {st1.Step}");
            }
        }
    }

    class Student
    {
        private string name;
        private int step;

        public string Name
        {  get { return name; } set { name = value; }   }

        public int Step
        {  get { return step; } set { step = value; } }

        public Student(string name, int step)
        {   this.name = name; this.step = step; }
    }
```
### Создать класс Person с полями имя и ID. Ограничение class для поля ID. Создать экземпляр с нужным типом и вывести на консоль
```C#
// Определяем класс Person
class Person<T> where T : class
{

    public string Name { get; set; }  // Поле для имени
    public T ID { get; set; }         // Поле для ID с ограничением class
    
    public Person(string name, T id)
    {
        Name = name;
        ID = id;
    }

    public void Display()
    {
        Console.WriteLine($"Name: {Name}, ID: {ID}");
    }
}

class Program
{
	static void Main()
    {
        // Создаем экземпляр класса Person с типом ID = string
        Person<string> person = new Person<string>("Alice", "A12345");
        // Выводим информацию на консоль
        person.Display();}
    }
```
### описать класс Code с полем Id c обобщенным типом. Описать дочерний класс Person c полями name, id(от класса code), grup тоже обобщенный
```C#
// Обобщенный базовый класс Code
public class Code<T>
{
    public T Id { get; set; }
    
    // Конструктор
    public Code(T id)
    {
        Id = id;
    }

    public void Display()
    {
        Console.WriteLine($"Code ID: {Id}");
    }
}

// Обобщенный класс Person, наследующий от Code
public class Person<T, U> : Code<T>
{
    public string Name { get; set; }
    public U Group { get; set; }

    // Конструктор
    public Person(T id, string name, U group) : base(id)
    {
        Name = name;
        Group = group;
    }

    public void DisplayPersonInfo()
    {
        Console.WriteLine($"Name: {Name}, ID: {Id}, Group: {Group}");
    }
}

class Program
{
    static void Main()
    {
        // Создание объекта Person с обобщенными типами
        Person<int, string> person1 = new Person<int, string>(1, "John Doe", "Group A");
        person1.DisplayPersonInfo();
        
        // Создание другого объекта Person с другими типами
        Person<string, double> person2 = new Person<string, double>("A123", "Jane Smith", 2.5);
        person2.DisplayPersonInfo();
    }
}
```
### создать класс где id обобщённый тип
```C#
// Обобщенный класс Person с полем Id
public class Person<T>
{
    public T Id { get; set; }

    // Конструктор
    public Person(T id)
    {
        Id = id;
    }

    public void DisplayInfo()
    {
        Console.WriteLine($"Person ID: {Id}");
    }
}

class Program
{
    static void Main()
    {
        // Создание объекта Person с типом int
        Person<int> person1 = new Person<int>(1);
        person1.DisplayInfo(); // Выводит: Person ID: 1

        // Создание объекта Person с типом string
        Person<string> person2 = new Person<string>("A123");
        person2.DisplayInfo(); // Выводит: Person ID: A123
    }
}
```
### обобщенный класс, поле фамилия и обобщенный тип код, в main() записать двумя способами string u int прибавить 1 и вывести результаты
```C#
// Обобщенный класс с полем LastName и обобщенным типом Code
public class Person<T>
{
    public string LastName { get; set; }
    public T Code { get; set; }

    // Конструктор
    public Person(string lastName, T code)
    {
        LastName = lastName;
        Code = code;
    }

    // Метод для прибавления 1 к значению поля Code (если это возможно)
    public void AddOne()
    {
        if (Code is int)
        {
            Code = (T)(object)((int)(object)Code + 1); // Если тип T - int, прибавляем 1
        }
        else if (Code is string)
        {
            Code = (T)(object)((string)(object)Code + "1"); // Если тип T - string, добавляем "1"
        }
    }

    // Метод для вывода информации
    public void DisplayInfo()
    {
        Console.WriteLine($"Last Name: {LastName}, Code: {Code}");
    }
}

class Program
{
    static void Main()
    {
        // Пример с типом int
        Person<int> person1 = new Person<int>("Ivanov", 100);
        person1.DisplayInfo(); // Выводит: Last Name: Ivanov, Code: 100
        person1.AddOne(); // Прибавляем 1 к полю Code
        person1.DisplayInfo(); // Выводит: Last Name: Ivanov, Code: 101

        // Пример с типом string
        Person<string> person2 = new Person<string>("Petrov", "Code");
        person2.DisplayInfo(); // Выводит: Last Name: Petrov, Code: Code
        person2.AddOne(); // Прибавляем 1 (или добавляем "1" в строку)
        person2.DisplayInfo(); // Выводит: Last Name: Petrov, Code: Code1
    }
}
```
### Класс Персона(фам, имя, отч).Сделать экземпляр в main.Записать фио через индексатор.
```C#
    class Program
    {
        static void Main(string[] args)
        {
            Person p = new Person();
            p["FIO"] = "F I O";
            Console.WriteLine($"{p.F}");
            Console.WriteLine($"{p.I}");
            Console.WriteLine($"{p.O}");
        }
    }

    class Person
    {
        private string f;
        private string i;
        private string o;
        public string F { get { return f; } set { f = value; } }
        public string I { get { return i; } set { i = value; } }
        public string O { get { return o; } set { o = value; } }
        public string this[string fullName]
        { set
            {
                var fio = value.Split(' ');
                f = fio[0];
                i = fio[1];
                o = fio[2];
            }
```
### Описать класс Person с полями Фамилия и Стипендия. Описать конструктор с параметрами для инициализации полей и свойства - для вывода.  B Main создать пять экземпляров этого класса. Используя их, составить массив и вычислить сумму стипендий, которую вывести на консоль.
```C#
class Program
    {
        static void Main(string[] args)
        {
            Person[] person = new Person[5];
            person[0] = new Person("Ivan", 1000);
            person[1] = new Person("Ivan", 2000);
            person[2] = new Person("Ivan", 3000);
            person[3] = new Person("Ivan", 4000);
            person[4] = new Person("Ivan", 5000);
            int total = 0;
            foreach (var p in person)
            { total += p.Step; }
            Console.WriteLine($"{total}");
        }
    }

    class Person
    {
        private string name;
        private int step;
        public string Name
        {
            get { return name; }
            set { name = value; }
        }
        public int Step
        {
            get { return step; }
            set { step = value; }
        }
        public Person(string name, int step)
        {
            this.name = name;
            this.step = step;
        }
    }
```
### Описать класс Person с полями: Фамилия и Код, для Кода использовать обобщенный тип с ограничением struct. B Main создать экземпляр, применяя для Кода допустимый тип, и вывести поля экземпляра на консоль.
```C#
class Program
    {
        static void Main()
        {
            Person<int> p1 = new Person<int>("Ivan", 1);
            p1.Info();
            Person<int> p2 = new Person<int>("Fedor", 124);
            p2.Info();
        }
    }
    class Person<T> where T:struct
    {
        private string name;
        private T code;
        public string Name
        {
            get { return name; }
            set { name = value; }
        }
        public T Code
        {
            get { return code; }
            set { code = value; }
        }
        public Person(string name, T code)
        {
            this.name = name;
            this.code = code;
        }
        public void Info()
        {
            Console.WriteLine($"{Name} - {Code}");} 
        }
    }
}
```
### 3.	реализовать частичный класс содержащий поле (фамилия) и конструктор для инициализации этого поля. В другом пакете содержится частичный метод для вывода на консоль фамилии
```C#
namespace MyApplication
{
    public partial class Person
    {
        private string surname;

        // Конструктор для инициализации поля
        public Person(string surname)
        {
            this.surname = surname;
        }
    }
}


namespace MyApplication.Extensions
{
    public partial class Person
    {
        // Частичный метод для вывода фамилии
        partial void PrintSurname();

        // Реализация метода
        partial void PrintSurname()
        {
            Console.WriteLine($"Surname: {surname}");
        }

        // Метод для вызова частичного метода
        public void DisplaySurname()
        {
            PrintSurname();
        }
    }
class Program
{
    static void Main(string[] args)
    {
        // Создание объекта Person
        Person person = new Person("Ivanov");

        // Вызов метода для отображения фамилии
        person.DisplaySurname();
    }
}

```